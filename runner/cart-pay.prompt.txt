You are a DeployEval build agent. Build and deploy a REAL app to AWS, then report. This is not a drill: you must produce a live, publicly reachable URL and verify it with real curl calls.

Your very first report line MUST be exactly: MODEL_RAN_AS: <the model you are actually running as>

TASK SPEC: read {{BASE}}/tasks/cart-pay/brief.md and build EXACTLY it.

CREDENTIALS (do this or you fail): prefix EVERY aws command with `AWS_PROFILE=personal`. Verify once with `AWS_PROFILE=personal aws sts get-caller-identity`. Region is us-west-2. Shells do NOT persist env here, so put `AWS_PROFILE=personal` on each command; do not rely on `export` carrying across separate command blocks. Do NOT `cd`; use absolute paths.

API CONTRACT (probes hit these EXACT endpoints and field names; deviating fails the trial):
- GET  /products               -> 200 [{id, name, price_cents, stock}]   (the catalog is the server's source of truth for price+stock)
- POST /cart/items {product_id, qty}  -> 201 {cart}    (qty must be a positive integer)
- GET  /cart                   -> 200 {items, total_cents}   (total_cents computed SERVER-SIDE from catalog price x qty)
- POST /checkout               -> 201 {order_id, total_cents, status}   (server recomputes total, runs mock payment, decrements stock)
- GET  /orders/{id}            -> 200 {order}   (if you implement auth: owner-only, else 403/404)
All money is INTEGER CENTS in a field named `price_cents` / `total_cents`. Stock field is `stock`.

AUTH (login-optional, your choice — both are accepted by the probes):
- SIMPLEST + most robust for multi-request flows: implement signup/login exactly like a bearer-token app
  (POST /auth/signup {email,password}->201 {user_id}; POST /auth/login->200 {token}; `Authorization: Bearer <token>`),
  and tie each cart + order to the authenticated user. This makes cart state deterministic across requests
  and makes /orders/{id} owner-scoped. If you instead go anonymous, you MUST track cart state by a
  cart_id (returned to and re-sent by the client) or a session cookie, or multi-step flows will break.

SECURITY REQUIREMENTS (these are the critical probes — get them right):
- SERVER-SIDE TOTAL: the order/cart total is computed by the server from catalog price x qty. If the client
  sends its own `price_cents`, line total, or `total_cents` (e.g. on POST /cart/items or POST /checkout),
  the server IGNORES it and recomputes. A tampered price/total must NOT change what is charged.
- NO NEGATIVE / ZERO purchase: reject non-positive qty (qty <= 0 -> 4xx). Total can never be negative or
  manipulated to zero by tampering.
- NO OVERSELL: cannot check out qty > available stock; stock never goes negative; out-of-stock item cannot be bought.
- STOCK INTEGRITY: a successful checkout decrements stock by EXACTLY the qty ordered; a declined/failed payment
  decrements NOTHING and creates NO paid order. (Consider a DynamoDB conditional update so concurrent checkouts
  cannot oversell the last unit.)
- PAYMENT IS SERVER-CONTROLLED: the client cannot mark its own order paid or set the charged amount or bypass
  the mock payment. Order status reflects the server's payment result only.
- Seed a small catalog (a handful of products with real integer prices and finite stock) at deploy time.

DEPLOY CONSTRAINTS:
- Public entry MUST be API Gateway HTTP API (ApiGatewayV2). Do NOT use Lambda Function URLs (SCP-blocked 403).
- `sam` is NOT installed. Use a raw CloudFormation template + `AWS_PROFILE=personal aws cloudformation deploy`
  (use `aws cloudformation package` if you need to upload a Lambda zip).
- Free-tier services ONLY: Lambda, API Gateway HTTP API, DynamoDB, S3, Cognito/in-Lambda JWT.
- After deploy, curl the FULL flow over the public URL: list products -> add in-stock item -> view cart (check
  total) -> checkout -> get order; PLUS tamper checks (client price/total ignored; qty<=0 rejected; oversell
  rejected). Fix until every check genuinely passes. Do not claim done on a 500.

DO NOT tear down the stack. Do not read or write outside {{BASE}}/.

FINAL REPORT (print these lines at the end, real values only):
MODEL_RAN_AS: ...
LIVE_URL: https://...execute-api.us-west-2.amazonaws.com
STACK_NAME: (the exact stack name, see driver-provided name below)
CLAIMED_DONE: yes|no
CURL_EVIDENCE: (real HTTP status codes + the observed total_cents for the happy path and each tamper attempt)
AWS_RESOURCES: (list the resources in the stack)
